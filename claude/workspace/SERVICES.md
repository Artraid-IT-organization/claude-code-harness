---
title: SERVICES — снапшот сервисов сервера
generated: файл генерируется автоматически, руками не править
---

# SERVICES.md

<!-- Этот файл ГЕНЕРИРУЕТСЯ скриптом ~/scripts/gen_services.py (поставь его в cron).
     Здесь лежит заглушка: до первого прогона содержимого нет.

     Зачем он нужен: ассистент постоянно спрашивает «а что вообще крутится на сервере?».
     Без снапшота он либо гадает, либо каждый раз обходит systemd, docker и cron заново —
     это дорого по контексту и всё равно неполно. Сгенерированный файл читается по запросу,
     а не каждую сессию.

     Что собирает скрипт: юниты systemd (системные и пользовательские), контейнеры docker,
     задания cron, слушающие порты, подключённые MCP-серверы, свободное место и аптайм.
     Значения вида *_TOKEN=, *_KEY=, *_PASSWORD= маскируются при сборе. -->

Снапшот ещё не сгенерирован.

Первый прогон:

```
python3 ~/scripts/gen_services.py
```

Регулярно — через cron, например ежедневно рано утром:

```
10 5 * * * timeout 10m /usr/bin/python3 $HOME/scripts/gen_services.py >> $HOME/logs/gen_services.log 2>&1
```
