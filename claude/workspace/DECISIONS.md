# DECISIONS — очередь решений владельца

<!-- Правила файла:
     • Один блок = один вопрос: «## [id] дата — заголовок», внутри: Вопрос / Дефолт (что будет,
       если не ответить) / Детали (ссылка на отчёт, НЕ пересказ).
     • Пишут сюда: раннеры патрулей (upsert по id через ~/.claude/hooks/decision-queue-upsert.sh),
       воркеры и ассистент (руками). Один источник = один блок, обновляется, не плодится.
     • Решено → ассистент в сессии удаляет блок по слову владельца (решение уходит в память/доки,
       не остаётся здесь). Файл — очередь, не архив.
     • SessionStart-хук показывает только СЧЁТЧИК — содержимое разворачивается по согласию. -->

<!-- Очередь пуста. Первый вопрос появится здесь сам, когда ассистенту понадобится твоё решение. -->
